name: Python Tests
on: [push, pull_request]

jobs:
  test: LoginFeature.py
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-python@v2

      - name: Install & Test
        run: |
          pip install -r requirements.txt
          python -m pytest
